# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code (per-user config)
.claude/

# Build
node_modules
dist
dist-ssr
*.local

# Env
.env
.env.local
.env.*.local

# Coverage
coverage

# Misc
*.tsbuildinfo

# 3D models — on whitelist explicite. Les .glb peuvent peser plusieurs
# dizaines de MB ; GitHub bloque les fichiers > 100 MB.
# - classic (1 MB) : whitelisted, prod-ready
# - rose (22 MB) : whitelisted, pushable mais lourd → à compresser
# - amp (8.5 MB) : whitelisted, ok mais idéalement < 1.5 MB
# - studio-scene (110 MB) : BLACKLISTED, dépasse la limite GitHub. Local
#   uniquement tant que pas compressé via gltf.report. Le hero affichera
#   son fallback gradient sur Vercel jusqu'à passage en prod.
public/models/*.glb
!public/models/guitar-fender-classic.glb
!public/models/guitar-fender-rose.glb
!public/models/amp.glb

# Screenshots — les placeholders légers (~ kB chacun) sont committés pour
# éviter les 404 dans le README GitHub. Quand Melvin lance scripts/
# screenshots.mjs avant le ship, les vrais PNG haute-res écrasent les
# placeholders. Si ces vrais PNG dépassent 1 MB chacun, retire-les ou
# convertis en WebP avant commit.
